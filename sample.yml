---
- hosts: all
  become: yes
  tasks:
      - name: i want to install tree
        apt:
           name: tree
           state: present
      - name: install apache2
        apt:
           name: apache2
           state: present
